{
  "type": "object",
  "properties": {
    "decision": {
      "type": "string",
      "enum": ["eligible", "ineligible", "ask_days", "ask_membership", "no_context"],
      "description": "The eligibility decision or the missing information needed to decide. no_context means no relevant policy was provided."
    },
    "text": { "type": "string", "description": "The customer-facing answer or clarification question." }
  },
  "required": ["decision", "text"],
  "additionalProperties": false
}
